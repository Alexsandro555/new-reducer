import { GLOBAL } from "@/constants";
import { ACTIONS, PRIVATE } from "@news/constants"
import { api } from "../../api/News";

export default {
    /*[ACTIONS.SAVE]: ({commit, store},data) => {
    }*/
}